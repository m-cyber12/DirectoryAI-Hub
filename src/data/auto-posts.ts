import type { BlogPost } from '@/data/posts';

/**
 * Auto-generated blog posts (idea #3 — AI blogging on autopilot).
 *
 * This file is the ONLY part of the blog that `scripts/generate-blog.mjs`
 * edits. Hand-written editorial posts in `src/data/posts.ts` are never
 * touched by the generator, so automation can't clobber human content.
 *
 * posts.ts spreads this array in front of the hand-written one, so the newest
 * generated post automatically becomes the featured/lead item.
 */
export const AUTO_BLOG_POSTS: BlogPost[] = [];

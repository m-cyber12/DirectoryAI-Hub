'use client';
/* eslint-disable @next/next/no-img-element */
import { useEffect, useMemo, useRef, useState } from 'react';
import { Download, Film, ImageIcon, MonitorCheck, Smartphone, Video } from 'lucide-react';
const human=(n:number)=>n<1024*1024?`${(n/1024).toFixed(1)} KB`:`${(n/1024/1024).toFixed(2)} MB`;
export function VideoInspector(){const[file,setFile]=useState<File|null>(null),[url,setUrl]=useState(''),[meta,setMeta]=useState<{w:number;h:number;duration:number}|null>(null),[poster,setPoster]=useState(''),video=useRef<HTMLVideoElement>(null);useEffect(()=>()=>{if(url)URL.revokeObjectURL(url);if(poster)URL.revokeObjectURL(poster)},[url,poster]);const select=(f?:File)=>{if(!f)return;setFile(f);setMeta(null);setPoster('');setUrl(URL.createObjectURL(f))};const ratio=meta?meta.w/meta.h:0;const fit=useMemo(()=>{if(!meta)return[];const vertical=ratio<.9,wide=ratio>1.5;return[{name:'YouTube Shorts',ok:vertical,text:vertical?'Vertical format fits Shorts.':'Use a 9:16 crop for Shorts.'},{name:'TikTok',ok:vertical,text:vertical?'Vertical format fits TikTok.':'A vertical 9:16 crop will fit better.'},{name:'YouTube',ok:wide,text:wide?'Wide format fits standard YouTube.':'Use a 16:9 version for standard YouTube.'}]},[meta,ratio]);const frame=()=>{const v=video.current;if(!v||!meta)return;const c=document.createElement('canvas');c.width=meta.w;c.height=meta.h;c.getContext('2d')!.drawImage(v,0,0);c.toBlob(b=>{if(b)setPoster(URL.createObjectURL(b))},'image/jpeg',.92)};const downloadPoster=()=>{if(!poster)return;const a=document.createElement('a');a.href=poster;a.download=`${file?.name.replace(/\.[^.]+$/,'')||'video'}-poster.jpg`;a.click()};return <div className="mx-auto grid max-w-7xl gap-6 px-4 pb-20 sm:px-6 lg:grid-cols-[.8fr_1.2fr]"><section className="studio-form-card"><h2>Select local video</h2><label className="studio-file-input"><Video className="h-5 w-5"/><span>{file?file.name:'Select an MP4, WebM, or MOV file'}</span><input aria-label="Select video file" type="file" accept="video/*" onChange={e=>select(e.target.files?.[0])}/></label>{file&&<div className="studio-meta-grid"><span>{human(file.size)}</span><span>{file.type||'Unknown type'}</span></div>}{meta&&<><div className="studio-video-facts"><span>Resolution<strong>{meta.w} × {meta.h}</strong></span><span>Aspect ratio<strong>{ratio.toFixed(2)}:1</strong></span><span>Duration<strong>{meta.duration.toFixed(1)} sec</strong></span></div><button className="studio-generate" onClick={frame}><ImageIcon className="h-4 w-4"/> Extract current poster frame</button><p className="studio-help">This reads metadata and extracts a still frame locally. It does not compress or transcode your video.</p></>}</section><section className="studio-results">{url?<><div className="studio-results-heading"><div><p className="studio-eyebrow">Local inspection</p><h2>{meta?'Platform fit':'Reading video metadata…'}</h2></div><span>Browser-only</span></div><video ref={video} src={url} controls className="studio-video-preview" onLoadedMetadata={e=>{const v=e.currentTarget;setMeta({w:v.videoWidth,h:v.videoHeight,duration:v.duration})}}><track kind="captions" /></video>{meta&&<div className="studio-fit-grid">{fit.map(x=><article key={x.name} data-ok={x.ok}><div>{x.ok?<MonitorCheck className="h-4 w-4"/>:<Smartphone className="h-4 w-4"/>}<strong>{x.name}</strong></div><p>{x.text}</p></article>)}</div>}{poster&&<div className="mt-5"><div className="flex items-center justify-between gap-3"><h3 className="text-sm font-bold">Poster frame</h3><button className="studio-copy" onClick={downloadPoster}><Download className="h-4 w-4"/> Download JPG</button></div><img className="studio-poster" src={poster} alt="Extracted poster frame"/></div>}</>:<div className="studio-empty"><Film className="h-7 w-7"/><p>Select a local video to see its duration, dimensions, aspect ratio, target-platform guidance, and an optional poster frame.</p></div>}</section></div>}

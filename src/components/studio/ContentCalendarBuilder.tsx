'use client';
/* eslint-disable jsx-a11y/label-has-associated-control */
import { useMemo, useState } from 'react';
import { Copy, Download, Plus, Sparkles } from 'lucide-react';
import { StudioSelect } from './StudioSelect';
type Row={date:string;platform:string;format:string;topic:string;hook:string;cta:string;status:string};
const ideas=['A practical lesson from','A common mistake in','A behind-the-scenes look at','A simple framework for','What I learned about','A quick myth check for'];
const formats=['Short video','Carousel','Tutorial','Behind the scenes','Case study','Q&A'];
const toCSV=(rows:Row[])=>['Date,Platform,Format,Topic,Hook,CTA,Status',...rows.map(r=>[r.date,r.platform,r.format,r.topic,r.hook,r.cta,r.status].map(x=>`"${x.replaceAll('"','""')}"`).join(','))].join('\n');
export function ContentCalendarBuilder(){const[f,setF]=useState({niche:'',platforms:'YouTube',frequency:3,goal:'Growth',days:7,pillars:''});const[rows,setRows]=useState<Row[]>([]);const make=()=>{const start=new Date();const platforms=f.platforms.split(',').map(x=>x.trim()).filter(Boolean);const pillars=f.pillars.split(',').map(x=>x.trim()).filter(Boolean);const total=Math.min(f.days,Math.max(1,Math.round(f.days*f.frequency/7)));const next=Array.from({length:total},(_,i)=>{const d=new Date(start);d.setDate(start.getDate()+Math.round(i*7/f.frequency));const pillar=pillars[i%pillars.length]||f.niche||'your niche';const lead=ideas[i%ideas.length];return{date:d.toISOString().slice(0,10),platform:platforms[i%platforms.length]||'YouTube',format:formats[i%formats.length],topic:`${lead} ${pillar}`,hook:`A useful ${f.goal.toLowerCase()} angle for ${pillar}`,cta:f.goal==='Leads'?'Reply or visit the link for the next step':'Save this for your next post',status:'Idea'};});setRows(next)};const update=(idx:number,key:keyof Row,value:string)=>setRows(rs=>rs.map((r,i)=>i===idx?{...r,[key]:value}:r));const csv=useMemo(()=>toCSV(rows),[rows]);const copy=()=>navigator.clipboard.writeText(csv);const download=()=>{const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='creatorai-content-calendar.csv';a.click();URL.revokeObjectURL(a.href)};return <div className="mx-auto max-w-7xl px-4 pb-20 sm:px-6"><form className="studio-calendar-controls" onSubmit={e=>{e.preventDefault();make()}}><label>Niche<input className="studio-field" required value={f.niche} onChange={e=>setF({...f,niche:e.target.value})} placeholder="e.g. home cooking"/></label><label>Platforms <input className="studio-field" value={f.platforms} onChange={e=>setF({...f,platforms:e.target.value})} placeholder="YouTube, Instagram"/></label><label>Posts / week<StudioSelect ariaLabel="Posts per week" value={String(f.frequency)} onChange={value=>setF({...f,frequency:Number(value)})} options={[1,2,3,4,5,7].map((value)=>({value:String(value),label:`${value} posts`}))} /></label><label>Goal<StudioSelect ariaLabel="Goal" value={f.goal} onChange={value=>setF({...f,goal:value})} options={['Growth','Education','Leads','Authority','Entertainment'].map((value)=>({value,label:value}))} /></label><label>Calendar length<StudioSelect ariaLabel="Calendar length" value={String(f.days)} onChange={value=>setF({...f,days:Number(value)})} options={[7,14,30].map((value)=>({value:String(value),label:`${value} days`}))} /></label><label>Content pillars <input className="studio-field" value={f.pillars} onChange={e=>setF({...f,pillars:e.target.value})} placeholder="Optional, comma-separated"/></label><button className="studio-generate"><Sparkles className="h-4 w-4"/> Build calendar</button></form>{rows.length>0?<section className="studio-calendar-result"><div className="studio-results-heading"><div><p className="studio-eyebrow">Editable output</p><h2>{rows.length}-post content calendar</h2></div><div className="flex gap-2"><button className="studio-copy" onClick={copy}><Copy className="h-4 w-4"/> Copy CSV</button><button className="studio-copy" onClick={download}><Download className="h-4 w-4"/> Download</button></div></div><div className="studio-table-wrap"><table><thead><tr>{['Date','Platform','Format','Topic','Hook','CTA','Status'].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={i}>{(Object.keys(r) as (keyof Row)[]).map(key=><td key={key}><input aria-label={`${key} row ${i+1}`} value={r[key]} onChange={e=>update(i,key,e.target.value)}/></td>)}</tr>)}</tbody></table></div><button className="studio-add-row" onClick={()=>setRows([...rows,{date:'',platform:f.platforms.split(',')[0]||'',format:'',topic:'',hook:'',cta:'',status:'Idea'}])}><Plus className="h-4 w-4"/> Add row</button></section>:<section className="studio-empty studio-calendar-empty"><Sparkles className="h-7 w-7"/><p>Build a practical publishing plan from your own niche and pillars. This does not claim live trend research.</p></section>}</div>}

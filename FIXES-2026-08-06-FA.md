# اصلاحات کد + توضیحات — ۲۰۲۶-۰۸-۰۶

این فایل دو بخش دارد:

1. **باگ‌های کدنویسی که رفع شد** (بدون دست‌زدن به طراحی سایت).
2. **موارد باقی‌مانده که باید توضیح بدهم** — این‌ها یا تصمیم محتوایی/تجاری‌اند یا refactor بزرگ‌تر که عمداً به آن‌ها دست نزدم تا طراحی و عملکرد فعلی نشکند.

> ✅ قبل از پوش: `npm run verify` و `npm run build` هر دو با موفقیت اجرا شدند.
> نوع‌چک (tsc) ✔ | لینت ✔ | صحت داده (۲۰۶ ابزار) ✔ | ۳۷ تست واحد ✔ | بیلد ~۱۰۲۰ مسیر ✔

---

## بخش ۱ — باگ‌هایی که رفع شد

### ۱.۱ شمارنده‌های صفحه اصلی (SSR) — `src/app/page.tsx`
**مشکل:** در HTML خام سایت، چهار شمارنده بزرگ همیشه `0` بودند و فقط بعد از اجرای GSAP به عدد واقعی می‌رسیدند. خزنده‌ی گوگل، کاربران بدون JS و کاربران `prefers-reduced-motion` همیشه «۰ ابزار» می‌دیدند.
**رفع:** عدد واقعی (`206`, `37`, `0`, `17`) حالا مستقیم در SSR رندر می‌شود. انیمیشن count-up فقط polish بصری روی همان عدد است و دیگر منبع محتوا نیست.
**نتیجه:** در کد HTML خام: `data-count="206">206` و … ✅

### ۱.۲ لندمارک `<main id="main">` گمشده — `src/app/page.tsx` + ۱۲ صفحه دیگر
**مشکل:** لینک skip-link در layout به `#main` اشاره می‌کند ولی صفحه‌ی اصلی اصلاً `<main>` نداشت؛ یعنی کاربر کیبوردی «اسکیپ» به هیچ‌جا می‌خورد. چند صفحه‌ی دیگر هم `<main>` بدون id داشتند.
**رفع:** صفحه اصلی حالا در `<main id="main">` است؛ و به همه‌ی `<main>`های بدون id در صفحه‌های account، admin، blog، calculators، changelog، contact، developers، founders، login، privacy، submit، terms هم `id="main"` اضافه شد.

### ۱.۳ برچسب گمراه‌کننده «Last reviewed» — `src/app/tool/[slug]/page.tsx` و `CompareClient.tsx`
**مشکل:** برای ابزارهای تست‌نشده (۱۶۱ ابزار generated) مقدار `lastReviewed: 2026-08-01` با برچسب «آخرین بار بررسی شد» نمایش داده می‌شد — در حالی که هیچ بررسی واقعی انجام نشده بود.
**رفع:** حالا فقط ابزارهای واقعاً hands-on-tested برچسب «Last reviewed» را می‌بینند (با `testedAt`)؛ بقیه «Catalogued on» و «Pricing checked» را می‌بینند. در جدول مقایسه هم ستون به «Tested / Catalogued» تغییر کرد.

### ۱.۴ فرم ارسال ابزار — `src/app/api/submit/route.ts` (مهم)
**مشکل اول:** وقتی Supabase تنظیم نبود، API در «Mock Mode» با `success: true` جواب می‌داد و فرانت‌اند به کاربر می‌گفت «دریافت شد، ایمیل می‌زنیم» در حالی که هیچ‌چیز ذخیره نمی‌شد — **شکستِ خاموش با پیام موفقیت، بدترین نوع شکست است.**
**مشکل دوم:** نوشتن با کلاینت anon انجام می‌شد که بعد از migration 0003 (قفل RLS) به‌طور قطعی fail می‌شود.
**رفع:** نوشتن حالا از `supabaseAdmin` (سرویس‌رول) انجام می‌شود؛ و وقتی دیتابیس نباشد به‌جای success جعلی، خطای صریح `503` با پیام «هنوز فعال نیست، لطفاً ایمیل بزنید» برمی‌گردد.

### ۱.۵ بررسی‌های کاربری — `src/app/api/reviews/route.ts`
**مشکل:** `user_id` از body کلاینت پذیرفته می‌شد، یعنی هر کس می‌توانست ریویو را به حساب دلخواه منتسب کند.
**رفع:** `user_id` از body حذف شد؛ ریویوهای مهمان anonymous (null) ثبت می‌شوند تا session سمت سرور واقعاً پیاده شود.

### ۱.۶ API عمومی `/api/tools` — `src/app/api/tools/route.ts`
**مشکل:** این endpoint قدیمی عمومی، ردیف‌های database را با فیلدهایی مثل `rating` و `reviews_count` برمی‌گرداند — همان امتیازهای جعلی/قدیمی که سایت عمداً از UI حذف کرده. این با API v1 (evidence-aware) در تناقض بود.
**رفع:** پاسخ عمومی حالا از یک projection پاک عبور می‌کند و فیلدهای `rating`، `reviewsCount`، `ratingLabel`، `isTrending`، `metrics` را حذف می‌کند.

### ۱.۷ نشت اخبار pending از RLS — `supabase/migrations/0008_news_read_policy_approved_only.sql`
**مشکل:** migration 0005 سیاست خواندن عمومی `using (true)` داشت؛ migration 0006 ستون `approved` را اضافه کرد ولی policy را محدود نکرد. پس اخبار تأییدنشده (pending) از طریق Supabase REST عمومی قابل خواندن بود، حتی اگر UI آن را نشان نمی‌داد.
**رفع:** migration جدید 0008 policy خواندن را به `using (approved = true)` محدود می‌کند (مسیر سرویس‌رول برای admin دست‌نخورده می‌ماند).
> ⚠️ این migration را حتماً روی Supabase اعمال کن.

### ۱.۸ اسکرول سینمایی فقط برای صفحه اصلی — `src/components/SmoothScroll.tsx`
**مشکل:** SmoothScroll (Lenis + GSAP) در layout ریشه بود و برای همه‌ی صفحات directory یک rAF loop اضافه اجرا می‌کرد — برای صفحات ابزار/کاتالوگ که اسکرول choreography ندارند، هزینه‌ی عملکردی بیهوده بود.
**رفع:** لایه‌ی smooth-scroll فقط روی `/` فعال می‌شود؛ بقیه‌ی صفحات اسکرول native دارند. (طراحی بدون تغییر.)

### ۱.۹ دکمه‌های آیکن‌دار — `src/components/ToolActions.tsx`
**مشکل:** دکمه‌های bookmark/compare فقط `title` داشتند و `aria-label` صریح نداشتند (نابینایان نمی‌فهمیدند چی کار می‌کنند). state هم `aria-pressed` نداشت.
**رفع:** `aria-label` و `aria-pressed` اضافه شد (بدون تغییر ظاهری).

### ۱.۱۰ کیبورد برای جستجوی پیشنهادی — `src/components/HomeSearch.tsx`
**مشکل:** لیست پیشنهادها `role=listbox` داشت ولی ناوبری با کلیدهای جهتنما و `aria-activedescendant` نداشت؛ کاربر کیبوردی نمی‌توانست بین پیشنهادها حرکت کند.
**رفع:** ناوبری ArrowDown/ArrowUp/Enter + hover موش + `aria-activedescendant` و `aria-selected` اضافه شد.

### ۱.۱۱ متادیتا و canonical صفحه‌ی Stack Builder — `src/app/stack-builder/`
**مشکل:** صفحه `'use client'` بود و `metadata` خروجی نداشت؛ پس canonical در HTML به `/` برمی‌گشت نه `/stack-builder`.
**رفع:** صفحه به server page (با Metadata + canonical) و client component (`StackBuilderClient.tsx`) تقسیم شد.
**نتیجه:** `rel="canonical"` حالا `/stack-builder` است. ✅

### ۱.۱۲ Open Graph صفحه‌ای برای صفحات مهم
**مشکل:** `/best-of`، `/trending`، `/changelog`، `/founders`، `/calculators`، `/developers` در اشتراک‌گذاری، تایتل generic سایت را نشان می‌دادند.
**رفع:** به هر کدام `openGraph` مخصوص همان صفحه اضافه شد.
**نتیجه:** مثلاً `og:title` برای /best-of حالا «Best AI Tools for Video Creators 2026» است. ✅

---

## بخش ۲ — موارد باقی‌مانده (نیاز به تصمیم تو / کار بزرگ‌تر)

این‌ها **باگ کدنویسی به معنای «خراب شده» نیستند**، اما از دید دو فایل نقد، ایراد اعتماد/محتوایی دارند. عمداً به طراحی دست نزدم؛ بگویید تا یکی‌یکی انجام دهم.

### ۲.۱ صفحه‌ی `/compare` هنوز Client-only است (P0 برای SEO)
کل محتوای hub (هدر، جدول، لینک ابزارها) بعد از hydration در مرورگر رندر می‌شود و HTML خام تقریباً خالی است. برای صفحه‌ای که در nav اصلی است، گوگل نمی‌تواند جدول/لینک‌ها را ببیند.
**راه‌حل پیشنهادی:** یک Server Component برای مقایسه‌ی پیش‌فرض و انتخاب‌های URL بسازیم؛ Client فقط برای انتخاب/تعامل بماند؛ حداقل `h1`، توضیح و جدول پیش‌فرض در SSR باشند. این refactor میانه‌ای است — اگر بخواهید انجامش می‌دهم.

### ۲.۲ Founder Claim Form هنوز fake است (P0)
`FounderClaimForm` فقط ۸۰۰ms صبر می‌کند و پیام موفقیت نمایش می‌دهد؛ هیچ API/ایمیلی نیست.
**راه‌حل:** یا از nav/landing حذف شود، یا به فرم واقعی (API + email verification + admin queue) تبدیل شود. این تصمیم محصول است، نه باگ.

### ۲.۳ آگهی‌های admin و CSRF (P1)
- `/api/settings` یک `announcement_*` ذخیره می‌کند که هیچ کامپوننتی رندر نمی‌کند → admin فکر می‌کند کار کرده ولی سایت تغییر نمی‌کند (یا wire شود یا حذف).
- `issueCsrfToken`/`verifyCsrfToken` در `adminAuth.ts` تعریف شده‌اند ولی هیچ route آن‌ها را صدا نمی‌زند؛ کامنت «CSRF fixed» با implementation یکی نیست.
**راه‌حل:** افزودن verify واقعی به همه‌ی routes write و ارسال token از پنل admin. این refactor متوسطی است.

### ۲.۴ رتبه‌بندی بر اساس ratingهای پنهان
با اینکه UI امتیاز نمایش نمی‌دهد، منطق `searchToolsAdvanced`، sort پیش‌فرض، best-of، category sorting، alternatives، compare و deals هنوز از `rating`/`reviewsCount`/`isTrending` استفاده می‌کند. یعنی **سایت امتیاز را نشان نمی‌دهد اما تصمیم را بر اساس آن می‌گیرد** — برای integrity بدتر از نمایش با disclaimer است.
**راه‌حل:** حذف این سیگنال‌های بی‌source از ranking یا الزام `sourceUrl + sourceType + asOf` برای هرکدام. این یک تصمیم محصول/اعتماد است.

### ۲.۵ «Best of» در حالی که تست hands-on صفر است
صفحه «Best overall» را نمایش می‌دهد در حالی که بر اساس prominence و ratingهای seed است. تا وقتی حداقل ۳ ابزار در هر دسته تست نشده‌اند، بهتر است به «Research shortlist» تغییر نام دهد یا noindex شود.

### ۲.۶ خبرهای curated «real, plausible» (P0 اعتبار)
در `data/news.ts` نوشته شده «real, plausible». «قابل‌باور» معیار انتشار خبر نیست؛ لینک دقیق announcement/author ندارد. و `getNews()` وقتی snapshot تأییدشده دارد، آن را با `CURATED_NEWS` merge می‌کند (پس ادعای «همه چیز editorial-approved» درباره‌ی curated seed صادق نیست).
**راه‌حل:** یا curated news حذف/تبدیل به «Editorial Opinion» شود، یا فقط RSS items با URL واقعی نمایش داده شود، و curated fallback از feed عمومی جدا شود.

### ۲.۷ شماره‌های جعلی social proof در Changelog
جملات «All 198 tools verified» و «Join 3,400+ video creators» با واقعیت (۲۰۶ ابزار، صفر hands-on، سایت لانچ‌نشده) در تناقض است و دقیقاً همان social proof ساختگی‌ای است که برند قصد دارد علیه آن باشد. حذف یا اصلاح این اعداد تصمیم محتوایی است.

### ۲.۸ اعتیاد به محتوای خودکار (Blog generator + News aggregator)
وجود auto-blog و GitHub Action، در این niche می‌تواند در چند هفته صدها صفحه‌ی تکراری تولید کند و ریسک penalty گوگل (thin / programmatic content) را بالا ببرد. تا وقتی هر مقاله reviewer/منبع/claim audit ندارد، publish نکنید. این استراتژی محتوایی است.

### ۲.۹ `lastReviewed` در فیلد داده / sitemap / v1 API
من خودم برچسب UI را اصلاح کردم، ولی فیلد خود `lastReviewed` هنوز در `tools-extended.ts` برای ابزارهای untested همان `2026-08-01` است. اگر خواستید، می‌توانم در سورس داده هم به `cataloguedAt` تغییر نام دهم (این یک rename سراسری است و روی sitemap و v1 API هم اثر دارد).

### ۲.۱۰ فرم Newsletter
واقعاً ایمیل ارسال نمی‌شود (TODO) و confirmation token ساخته می‌شود ولی ارسال نمی‌شود. تا وقتی delivery end-to-end تست نشده، فقط «waitlist request recorded» بگویید، نه «check your inbox».

### ۲.۱۱ Iron Man / Infinity Gauntlet
از نظر IP/Trademark با Iron Man/Tony Stark/Doc Doom ریسک دارد و توجه را از جستجو و تصمیم می‌دزدد. پیشنهاد نقد: تبدیل به easter egg در footer یا بازطراحی با لورِکسی کاملاً اورجینال. این تصمیم طراحی/برند است — عمداً دست نزدم.

### ۲.۱۲ Security: ارتقای dependencies
`npm audit` سه آسیب‌پذیری high در زنجیره‌ی postcss/sharp گزارش می‌دهد؛ fix پیشنهادی نیاز به ارتقای breaking به Next 16 دارد. قبل از launch روی یک branch جدا تست شود. من این را انجام ندادم چون upgrade breaking است و می‌تواند طراحی/build را بشکند.

---

## توصیه‌ی اولویت‌بندی (خلاصه)

| اولویت | اقدام |
|---|---|
| 🔴 P0 | اعمال migration 0008 روی Supabase (امنیتی). |
| 🔴 P0 | اصلاح /compare به SSR (من آماده انجامم). |
| 🔴 P0 | حذف/واقعی‌کردن Founder Claim Form و `3,400+ creators`. |
| 🟠 P1 | حذف ratingهای پنهان از ranking یا sourceدارکردن آن‌ها. |
| 🟠 P1 | wire/حذف announcement admin + استفاده‌ی واقعی از CSRF. |
| 🟠 P1 | جداکردن curated news از feed تأییدشده. |
| 🟡 P2 | اصلاح اعداد changelog، فرم newsletter واقعی، ارتقای Next. |

اگر هر کدام را خواستید انجام بدهم بگویید — این refactorها را یک‌به‌یک روی همین مخزن اعمال می‌کنم.
